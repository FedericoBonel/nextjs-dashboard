import { handlers } from "@/lib/auth";

/** This exposes the required endpoints for Next-Auth, when using client side APIs */
export const GET = handlers.GET;

/** This exposes the required endpoints for Next-Auth, when using client side APIs */
export const POST = handlers.POST;
